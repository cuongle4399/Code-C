#include <stdio.h>
#include <math.h>
main()
{
	int a=100; int b=2; int c=-5;
	float k=exp(b);
	printf(" binh phuong cua e mu 2 %f \n",k);
	float j=sqrt(a);
	printf(" can bac 2 cua 100 %f \n",j);
	int h=abs(c);
	printf(" gia tri tuyet doi cua -5 %d \n",h);
	int v=pow(b,2);
	printf(" b mu 2 %d",v);
}
//t=exp(b); binh thuong cua e mu b
///i= fabs(b); giá trị tuyệt đối của số thực
//o= pow (b,x);  b mũ x
// m= abs(c); gia trị tuyệt đối của số nguyên

